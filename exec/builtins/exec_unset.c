#include "../../inc/header.h"

void    delete_env(env_list *env,  char *key)
{
    env_list    *tmp;
    env_list    *prev;

    tmp = (*env);
    prev == (*env);
    while (tmp)
    {
        if (ft_strncmp(tmp->key, key, ft_strlen(key)))
    }
}

void    exec_unset()
{

}